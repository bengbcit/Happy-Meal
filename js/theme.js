// theme.js — Theme management (future: dark/light toggle)
// テーマ管理 / 主题管理
const ThemeManager = {
  init() {
    // Currently follows OS preference via CSS media query
    // 現在はCSSメディアクエリでOSの設定に従う / 目前通过 CSS 媒体查询跟随系统设置
    // Future: add manual toggle button
  }
};
