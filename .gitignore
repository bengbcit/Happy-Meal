# Claude context — local only, not for GitHub
# Claudeコンテキスト — ローカルのみ / Claude 上下文文件，仅本地保存
CONTEXT.md

# Environment variables / 环境变量 / 環境変数
.env
.env.local
.env.production

# Node modules
node_modules/

# Vercel
.vercel/

# macOS
.DS_Store

# Editor
.vscode/
*.suo
.claude/

# OAuth client secrets — NEVER commit these
client_secret*.json
*OAuth*.json